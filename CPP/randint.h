// Function to find the sum of two
// numbers passed

#include <time.h>
#include <bits/stdc++.h>

// figure out how to setup the header file for all files

int randomizer()
{
    int number;

    srand(time(0));

    return rand();
    // return number
}